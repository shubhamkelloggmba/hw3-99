<p><%= link_to "Back to places", places_path %></p>

<h1><%= @place.name %></h1>

<% @entries.each do |entry| %>
  <div>
    <h3><%= entry.title %></h3>
    <p><%= entry.occurred_on %></p>
    <p><%= entry.description %></p>
  </div>
<% end %>

<p><%= link_to "New Post", new_place_entry_path(@place) %></p>
